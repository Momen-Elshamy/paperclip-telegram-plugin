{
  "name": "paperclip-telegram-plugin",
  "version": "1.0.0",
  "description": "Telegram notifications and messaging for Paperclip agents",
  "type": "module",
  "main": "dist/worker.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": "^2026.325.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0"
  }
}
